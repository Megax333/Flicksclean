{
  "mcpServers": {
    "supabase": {
      "command": "npx",
      "args": [
        "-y",
        "supabase",
        "--access-token",
        "REPLACE_WITH_YOUR_PERSONAL_ACCESS_TOKEN"
      ]
    }
  }
}
